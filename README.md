# kubectl plugin for IPs

This plugin displays the IP Addresses for your kubenretes cluster.

## Install

```bash
make install

# move this binary to any directory in your PATH
mv kubectl-ips ~/.local/bin
```

## Usage

```
kubectl ips
```
